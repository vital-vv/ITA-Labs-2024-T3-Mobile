import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  headerWrapper: {
    display: 'flex',
    flexDirection: 'row',
  },

  headerTitle: {
    display: 'flex',
    flexDirection: 'row',
  },
});
