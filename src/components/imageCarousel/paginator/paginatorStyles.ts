import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  dotsContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    position: 'absolute',
    bottom: '5%',
    alignSelf: 'center',
  },
});
