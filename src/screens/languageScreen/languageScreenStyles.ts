import {StyleSheet} from 'react-native';

const languageScreenStyles = StyleSheet.create({
  container: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    marginTop: 24,
  }
});

export default languageScreenStyles;
